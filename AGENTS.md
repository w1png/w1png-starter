Please utilize TDD when writing code.
First you make red tests then implement a backend feature, then ensure its green then go to frontend if required.

After every change do npm run check:types and npm run check:lint
